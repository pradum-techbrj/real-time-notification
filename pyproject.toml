[project]
name = "test-live-tracker"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]==0.40.0",
    "redis==7.3.0"
]

[tool.fastapi]
entrypoint = "main:app"